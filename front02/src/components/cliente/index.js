import { Typography } from '@mui/material';
import React from 'react';

// import { Container } from './styles';

function Cliente() {
  return (
    <Typography>pagina do cliente</Typography>
  );
}

export default Cliente;